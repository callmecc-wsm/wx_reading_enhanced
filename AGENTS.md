# 微信读书阅读增强插件 - 架构概述

## 项目概述

这是一个Chrome浏览器扩展原型，旨在为微信读书提供多列阅读增强功能。项目采用纯前端技术栈，通过Chrome扩展API实现弹出式界面，支持单列、双列、三列阅读模式切换，并模拟翻页功能。

### 核心功能
- 多列阅读模式（1/2/3列）
- 模拟翻页导航
- 跟随高亮显示当前阅读段落
- 响应式弹出界面设计

## 技术架构

### 文件结构
- `manifest.json` - Chrome扩展配置文件（Manifest V3）
- `popup.html` - 扩展弹出界面主页面
- `popup.js` - 核心交互逻辑和阅读控制
- `popup.css` - 界面样式和主题配置

### 技术特点
- **无依赖**: 纯原生JavaScript，无外部库依赖
- **模块化**: 使用ES6模块语法
- **响应式**: 固定尺寸弹窗（420x540px）
- **无障碍**: 完整的ARIA标签和键盘导航支持

## 开发指南

### 开发环境
- Chrome浏览器（支持Manifest V3）
- 本地开发服务器（可选）
- 代码编辑器

### 安装开发版本
1. 打开Chrome扩展管理页面（chrome://extensions/）
2. 启用"开发者模式"
3. 点击"加载已解压的扩展程序"
4. 选择项目根目录

### 调试方法
- 使用Chrome DevTools调试弹出页面
- 在`chrome://extensions/`页面查看错误日志
- 使用`console.log`进行客户端调试

## 代码规范

### 命名约定
- 变量/函数: camelCase（如`currentPage`, `setColumns`）
- CSS类名: kebab-case（如`reader-content`, `mode-option`）
- 常量: UPPER_CASE（如`PAGES`数组）
- 数据属性: kebab-case（如`data-columns`, `data-action`）

### 代码风格
- 使用单引号字符串
- 分号可选但保持一致
- 缩进使用2个空格
- 函数声明使用函数表达式

### 性能优化
- 使用`requestAnimationFrame`优化滚动事件
- 事件委托减少监听器数量
- CSS过渡使用硬件加速

## 安全考虑

### 内容安全
- 所有文本内容硬编码在JavaScript中
- 无外部API调用或数据获取
- 无用户输入处理

### 扩展权限
- 最小权限原则，仅使用基本扩展功能
- 无敏感权限申请
- 无跨域请求

## 测试建议

### 功能测试
- 验证三种列模式的切换功能
- 测试翻页按钮的循环导航
- 检查跟随高亮的滚动效果
- 验证弹窗的打开/关闭交互

### 兼容性测试
- Chrome版本兼容性（Manifest V3要求）
- 不同屏幕尺寸的显示效果
- 键盘导航的无障碍性

### 性能测试
- 检查滚动性能和平滑度
- 验证动画效果的流畅性
- 测试内存使用情况

## 部署发布

### 打包步骤
1. 确保所有文件在根目录
2. 检查`manifest.json`版本号
3. 创建ZIP压缩包（不包含.git目录）
4. 上传到Chrome Web Store开发者控制台

### 版本管理
- 遵循语义化版本控制（SemVer）
- 在`manifest.json`中更新版本号
- 记录版本变更日志

## 扩展开发

### 可能的增强方向
- 支持更多列数配置
- 添加字体大小调节
- 实现真实的微信读书集成
- 添加阅读进度保存
- 支持自定义主题颜色

### 技术限制
- 当前为独立原型，未与真实微信读书集成
- 固定尺寸弹窗，不支持自适应大小
- 模拟数据，无真实书籍内容